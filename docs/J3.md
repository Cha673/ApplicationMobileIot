# J3 — Sécuriser et fiabiliser le système IoT

**Situation :** le prototype fonctionne dans le cas nominal. Il faut maintenant vérifier qu’il reste cohérent quand les données sont incorrectes, qu’un capteur disparaît ou qu’un composant tombe en panne.

## Questions à résoudre

- Comment distinguer sans ambiguïté plusieurs capteurs simulés qui publient en parallèle ?

  Quand on a plusieurs capteurs, pour les identifier, il faut qu'ils aient chacun un identifiant unique et un timestamp qui permet de garder une empreinte temporelle. De plus, la hiérarchie des topics MQTT (par exemple telemetry/sensor-01) permet au broker et au backend de savoir immédiatement d'où vient le message sans même avoir à ouvrir et lire le contenu du JSON.

- Quelles données entrantes doivent être refusées avant d’atteindre le cœur du système ?

  Les données doivent être rejetées :
  - lorsque le message n'est pas un JSON valide ;

  - s'il manque un champ obligatoire : l'identifiant du device, le timestamp, la mesure ou encore l'identifiant unique de message (message_id) sans lequel on ne peut pas vérifier s'il s'agit d'un doublon ;

  - si le type de la mesure est incorrect, par exemple si la température n'est pas un nombre.

- Comment détecter qu’un capteur ne fournit plus une information suffisamment fraîche ?

  On détecte qu'un capteur ne fournit plus une information suffisamment fraîche lorsqu'il y a un trop grand laps de temps entre le timestamp du message (l'heure où le message est envoyé) et l'heure à laquelle le serveur le reçoit. De plus, grâce à un système de surveillance (watchdog), si un capteur n'envoie plus rien du tout pendant un certain temps, le serveur le détecte automatiquement comme inactif ou hors service, même sans recevoir de nouveau message.

- Comment le système se comporte-t-il lorsqu’un message est dupliqué, retardé ou invalide ?

  Lorsqu'un message dupliqué est transmis, le premier message passe normalement et s'enregistre dans MongoDB puis dans PostgreSQL. C'est la tentative d'insertion du deuxième message possédant le même message_id qui est rejetée grâce aux filtres de déduplication ($setOnInsert dans MongoDB et ON CONFLICT DO NOTHING dans PostgreSQL).

  Lorsqu'un message est retardé, on vérifie d'abord que le délai limite de fraîcheur n'est pas dépassé. Si la donnée est trop ancienne, elle n'est pas importée dans la base de données et un message d'avertissement est envoyé dans Grafana.

  Pour les messages invalides, le rejet génère immédiatement un log JSON structuré sur la sortie standard (stdout), qui est ensuite capturé par Promtail, envoyé dans Loki et affiché sous forme d'alerte ou de graphique sur Grafana.

- Que se passe-t-il si le broker MQTT devient indisponible puis revient ?

  Si le broker MQTT devient indisponible, le backend tente de se reconnecter automatiquement. Pendant cette perte de connexion, les messages envoyés s'empilent dans la file d'attente. Si celle-ci atteint sa limite, il y a une perte de messages qui peut survenir.

  Lorsque la connexion est rétablie, les capteurs envoient les messages présents dans la file d'attente.

- Quels secrets et paramètres de configuration ne doivent jamais être versionnés dans Git ?

  Dans Git, on ne versionne pas les identifiants et mots de passe des bases de données, les identifiants de connexion MQTT, les secrets et clés d'API, les certificats ou clés privées TLS/SSL, ainsi que les adresses ou URL de production sensibles. L'ensemble de ces paramètres doit être géré via des fichiers .env strictement ignorés par le fichier .gitignore.

## Scénarios à tester

### Isolation des devices : au moins deux capteurs simulés publient en parallèle sans mélange de données. Les logs permettent de suivre séparément chaque deviceId.

1. **Hypothèse**
   Quand au moins deux capteurs simulés publient en parallèle, ile ne devraient pas mélanger de données. Chaque message devrait contenir son propre ID, ce qui permet de ne pas mélanger les messages dans le backend.

2. **Injection**

![Scenario1 - Injection](images/Scenario1-Injestion.pngScenario1-Injection.png)

J'ai lancé la commande "docker compose run --rm tools watch --topic "campus/v1/devices/+/telemetry" --count 12", afin de voir plusieurs messages de l'ensemble des capteurs envoyés en même temps.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

![Scenario1 - Observation](images/Scenario1-Observation.pngScenario1-Observation.png)

On peut observer lors du test d'envoi de message, ainsi que dans les logs que chaque message a un device_id qui permet de ne pas les mélanger, ainsi qu'un messa_id pour que chaque message ait un id unique.

4. **Explication**

   MQTT sépare les données grâce à un topic contenant le device_id. Le backend récupère le device_id présent dans le message et stocke chaque message qui a cet id. Les requêtes PostgreSQL et MongoDB filtrent les messages par device_id. Cela permet à l'API d'associer chaque donnée, à sa salle et son capteur.

5. **Décision**

   Le comportement associé est conforme aux attentes. Aucune modification n'est nécessaire.

### Usurpation d’un device : publier manuellement un message sur le topic d’un autre capteur et observer si le système l’accepte. L’équipe explique ce qui empêche, ou n’empêche pas encore, un client de publier à la place d’un device et met en place une mitigation adaptée si nécessaire.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Payload invalide : envoyer un JSON invalide, un message incomplet puis une valeur incohérente ou impossible. Aucun de ces cas ne doit provoquer de crash silencieux ; le rejet doit être observable et expliqué.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Doublon : envoyer deux fois exactement le même événement et vérifier l’effet sur le stockage et l’état courant.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Désordre et replay : envoyer plusieurs événements dans un ordre temporel différent de leur ordre de production, puis rejouer un ancien événement. L’équipe détermine quelle donnée doit représenter l’état courant et pourquoi.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Capteur silencieux : arrêter brutalement un simulateur et mesurer le délai avant que la donnée ne soit considérée comme ancienne, silencieuse ou hors ligne selon la règle retenue.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Broker indisponible : arrêter le broker MQTT, observer précisément le comportement du simulateur et du backend, puis redémarrer le broker et prouver la reprise.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Backend indisponible : arrêter le backend pendant que les capteurs continuent de publier, le redémarrer puis déterminer quelles mesures ont été perdues, rejouées ou récupérées.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### QoS MQTT : exécuter un même scénario de coupure/reprise au minimum en QoS 0 puis en QoS 1. Comparer le nombre de messages envoyés, reçus, perdus ou éventuellement dupliqués et expliquer les garanties réellement observées.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Message retained : publier une mesure retained, arrêter le capteur puis redémarrer le consommateur. Vérifier si une ancienne valeur est reçue immédiatement et expliquer comment le système évite de la confondre avec une mesure fraîche.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.

### Montée en charge locale : augmenter progressivement la fréquence d’émission des simulateurs et observer la latence, les erreurs et le premier composant qui se dégrade. L’objectif est d’identifier une limite mesurée, pas d’atteindre un chiffre imposé.

1. **Hypothèse** annoncer ce qui devrait se produire avant d’exécuter le test.
2. **Injection** provoquer volontairement le défaut ou la situation étudiée.

3. **Observation** conserver les traces et mesurer ce qui s’est réellement produit.

4. **Explication** expliquer le comportement observé en s’appuyant sur le protocole, MQTT et l’architecture du système.

5. **Décision** décider si le comportement est acceptable ou si une modification est nécessaire.

6. **Vérification** après modification, rejouer exactement le même scénario et conserver la preuve du nouveau comportement.
